\section{Chapter 3:Cyber-Physical Experiments and Results}
This chapter presents a comprehensive empirical evaluation of proposed Deep Reinforcement Learning (DRL)-drivern Joint Communication and Control (JCC) 
framework. A fundamental objective of this study is to demonstrate that proposed intelligent bandwidth allocation strategy is not merely overfitted to a specific 
robotic configuration, but rather serves as universal, \textbf{cross-morphology generalization framework} capable of addressing extreme communication bottlenecks across diverse cyber-physical paradigms.

To rigorously validate this universality without relying on hardware-specific idiosyncrasies, the experimental methodology is structured around a progressive, tri-fold simulation and Digital Twin validation approach:
\begin{itemize}
    \item \textbf{High-DOF Fully-Actuated Spatial Allocation}: Utilizing a 7-DOF Franka Emika Panda manipulator to demonstrate the framework's capability to extract maximum spatial precision and execute "graceful degradation" under severe quantization noise.
    \item \textbf{Underactuated High-Dynamic Survival}: Employing a 12-dimensional Quadrotor UAV simulation subjected to extreme wind disturbances to validate the agent's pragmatic "survival prioritization" capabilities in life-or-death control scenarios.
    \item \textbf{Digital Twin and Robustness Stress Testing}: Leveraging a precise Digital Twin of a resource-constrained 5-DOF manipulator to conduct software-in-the-loop stress tests. This validates the algorithm's robustness against unmodeled dynamics, such as asymmetric payload variations and simulated actuator jitter, proving its readiness for edge deployment. 
\end{itemize}
Through these progressive scenarios, the empirical results will highlight the fundamental limitations of traditional Shannon-separated uniform transmission and static mathematical allocations, 
ultimately proving the superiority of context-aware dynamic resource scheduling.
\subsection{Experimental Setup and Implementation Details}
To comprehensively evaluate the performance and robustness of the JCC architecture, a rigorous multi-stage validation methodology 
was established. This section outlines the unified configuration of the simulation environments, the physical plants under investigation,
and the standardized evaluation baselines, ensuring the complete reproducibility of the experiments presented in this chapter.
\subsubsection{Simulation Engine and DRL Pipeline}
The primary testbeds for training the reinforcement learning agents and validating the extreme bandwidth constraints were constructed using the PyBullet physics engine. 
PyBullet was selected due to its highly optimized rigid-body dynamics, accurate collision detection algorithms, and robust support for simulating diverse robotic kinematics. 
To accurately capture the non-linear physical dynamics and transient tracking errors without introducing artificial simulator stability, the physics engine consistently operates with a base high-frequency control step of 240 Hz.

Due to the substantial computational overhead required for real-time Forward Kinematics (FK), Jacobian matrix calculations, and dynamic collision detection during the continuous DRL training phase, a multiprocessing vectorized architecture was implemented.
Utilizing the \inlinecode{SubprocVecEnv} wrapper from the \inlinecode{stable-baselines3} library, the training environments were parallelized 
across independent, headless PyBullet physics servers on a multi-core CPU. This parallelized paradigm enables the agent to explore 
hundreds of thousands of complex physical interactions simultaneously, drastically accelerating convergence while preventing the policy from overfitting 
to a single state trajectory.

The intelligent bit-allocation agents across all three scenarios were trained utilizing the Proximal Policy Optimization (PPO) algorithm under an Actor-Critic framework. The critical hyperparameters, tuned to balance exploration and stable trust-region updates, are detailed in 
\begin{table}[H]
\centering
\caption{Unified PPO Training Hyperparameters for JCC Bandwidth Allocation}
\label{tab:ppo_hyperparameters}
\renewcommand{\arraystretch}{1.5} % 增加行间距，使表格看起来更舒适
\begin{tabular}{@{}llp{8.5cm}@{}}
\toprule
\textbf{Hyperparameter} & \textbf{Value} & \textbf{Description} \\
\midrule
\textbf{Algorithm Framework} & PPO & Actor-Critic architecture for continuous action spaces. \\
\textbf{Network Architecture} & MLP [256, 256] & High-capacity hidden layers to process spatial coordinates. \\
\textbf{Total Timesteps} & 300,000 & Sufficient for hard-negative mining and physical stabilization. \\
\textbf{Learning Rate ($\alpha$)} & $3 \times 10^{-4}$ & Linear schedule decaying to zero for steady-state convergence. \\
\textbf{Discount Factor ($\gamma$)} & 0.99 & High emphasis on long-term reward maximization. \\
\textbf{Batch Size} & 64 & Number of experiences used for a single gradient update. \\
\textbf{Steps per Environment} & 512 & Rollout buffer size before updating the policy. \\
\textbf{Clip Range ($\epsilon$)} & 0.2 & Prevents destructively large policy updates; linearly decays. \\
\bottomrule
\end{tabular}
\end{table}
\subsubsection{Target Cyber-Physical Plants}
To scientifically prove that the proposed framework transcends specific hardware limitations, the evaluations were conducted across three fundamentally different physical plants:
\begin{itemize}
    \item \textbf{7-DOF Franka Emika Panda Manipulator}: A high-dimensional, fully-actuated, and kinematically redundant system. This plant is utilized to validate the agent's capacity for extreme spatial precision extraction under severe bandwidth bottlenecks, forcing the algorithm to learn the complex mechanical leverage effect between base and wrist joints.
    \item \textbf{Quadrotor UAV (12-Dimensional State)}: A classic underactuated and highly unstable dynamic system. Unlike a robotic arm that merely halts upon communication failure, a UAV losing spatial feedback instantly crashes. It is employed to demonstrate the framework's "Survival Prioritization" capability—sacrificing spatial coordinates to guarantee the transmission of critical attitude (roll/pitch) data under extreme wind disturbances.
    \item \textbf{Custom  Manipulator (Digital Twin \& Physical Hardware)}: A cost-effective, resource-constrained edge device. This customized plant serves as the ultimate Sim-to-Real validation testbed. A precise Digital Twin was constructed in PyBullet for pre-training, after which the policy was deployed to the physical hardware to test its robustness against real-world static friction and unmodeled payload dynamics.
\end{itemize}
\subsubsection{Baseline Methodologies}
To benchmark the superiority of the DRL-driven dynamic allocator, two standardized spatial allocation baselines were defined and uniformly applied across all experimental scenarios:
\begin{enumerate}
    \item \textbf{Uniform Allocation (Average)}: A naive, mathematically straightforward baseline that evenly distributes the total available bandwidth ($B_{total}$) across all $n$ control dimensions. While computationally zero-cost, it completely ignores the diverse physical sensitivities of different states, treating a critical base joint exactly the same as a distal wrist joint.
    \item \textbf{Static LQR-Weighted Allocation}: A state-of-the-art mathematical baseline. It utilizes the infinite-horizon Linear Quadratic Regulator (LQR) cost function and the system's static Jacobian sensitivity matrix (computed at the nominal home posture) to perform logarithmic water-filling bit assignment. While this scheme successfully prioritizes sensitive joints initially, it suffers heavily from "Jacobian Shifting"—its fixed formulas tend to collapse when the highly non-linear system operates far from the pre-calculated home posture.
\end{enumerate}
\subsection{Simulation Scenario I: High-DOF Spatial Allocation (Franka Manipulator)}\label{sec:s1}
To empirically validate the superiority of the proposed Deep Reinforcement Learning (DRL)-driven Joint Communication and Control (JCC) framework, 
a series of comprehensive simulations were conducted utilizing the high-fidelity PyBullet physical engine. 
This section systematically evaluates the framework's performance under severe bandwidth constraints, benchmarking the intelligent PPO-based dynamic allocator against traditional uniform allocation and static LQR-weighted allocation strategies on a high-DOF manipulator.
\subsubsection{Training Convergence and Reward Analysis}
Before evaluating the final tracking performance of the robotic manipulator, it is imperative to analyze the training dynamics and convergence behavior of the PPO agent. 
Training a DRL agent within a highly non-linear, rigid-body physics environment presents significant challenges, particularly due to the discontinuous nature of extreme bit quantization and the resulting high-variance physical feedback.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Figures/reward1.png}
    \caption{\small{Convergence curve of the PPO agent's competitive reward over 25 evaluation intervals. The curve perfectly illustrates the agent's progression from an initial heavily penalized exploratory phase to a successful algorithmic breakthrough where it surpasses the mathematical baselines, ultimately achieving smooth steady-state convergence.}}
    \label{fig:reward}
\end{figure}
As vividly captured in Figure.~\ref{fig:reward} the training convergence process exhibits a highly stable and monotonic ascent, which profoundly reflects the agent's complex learning behavior. This progression can be analyzed through three distinct phases:
\begin{itemize}
    \item \textbf{Initial Exploration and High Penalty Phase (Evaluations 0 to 5)}: In the initial stages of training, the Actor network's weights are randomly initialized, leading to stochastic bandwidth allocation across the 7-DOF joints. Because the agent has not yet learned the mechanical "lever effect" of the manipulator, its random policy performs significantly worse than the predefined Static LQR and Uniform baselines. Consequently, the agent receives a severe competitive penalty, initiating at roughly $-20$. This phase perfectly validates the necessity of the competitive reward shaping; the environment yields consistent, unyielding penalties, coercing the agent to desperately search for a viable gradient out of the sub-optimal allocation trap.
    \item \textbf{Rapid Ascent and Baseline Breakthrough Phase (Evaluations 5 to 15)}: Following the initial exploratory penalty, the reward curve exhibits a steep and continuous ascent. Driven by the 34-dimensional 3D spatial awareness injection, the PPO agent rapidly discovers the fundamental principle of \textit{Pragmatic Communication}: proactively stripping bits from robust, distal joints to unconditionally secure high-precision transmission for critical base joints. The most critical academic milestone occurs around \textbf{Evaluation 14}, where the reward curve successfully \textbf{crosses the zero-axis ($r>0$)}. In our reward formulation, crossing this threshold mathematically signifies that the DRL agent has officially surpassed the blended tracking performance of both the Static LQR and the Uniform average baselines, transitioning from a sub-optimal novice to a highly intelligent allocator.
    \item \textbf{Steady-State Convergence Phase (Evaluations 15 to 25)}: After decisively beating the theoretical baselines, the reward continuous to climb and gradually plateaus into a smooth steady-state, stabilizing at an asymptotic maximum of approximately $+2.5$. Notably, the curve exhibits no catastrophic forgetting or violent trust-region oscillations typical of complex continuous control tasks. This perfect, smooth convergence is heavily supported by the linear learning rate scheduling and the clipping mechanism implemented within into the PPO environment. This dynamic scheduling forces the agent to seamlessly transition from aggressive exploration to conservation exploitation, securely "locking in" the learned pragmatic allocation policy for extreme bandwidth scenarios.
\end{itemize}
\subsubsection{Spatial Domain: Scalability and Performance under Diverse Constraints}
Following the verification of the DRL agent's stable convergence, this experiment investigates the core spatial domain performance of the proposed Joint Communication and Control (JCC) framework.
The primary objective is to evaluate how effectively the intelligent agent arbitrates limited network resources across the 7-DOF manipulator compared to traditional heuristics, and crucially, how this performance scales across different degrees of channel degradation.

To comprehensively assess scalability and ensure that the RL policy generalizes rather than overfitting to a single maneuver, the evaluations were conducted across \textbf{three distinct pick-and-place trajectories}. The total bandwidth constraint ($B_{total}$) per sampling interval was systematically swept from a saturated regime (56 bits) down to an extreme "survival-level" bottleneck (14 bits).

The accurately simulate a real-world networked control system (NCS), the communication frequency was decoupled from the high-frequency 
physical control loop. The control commands were transmitted at 10 Hz over the simulated JCC channel using Differential Pulse Code Modulation (DPCM), while the PyBullet physics engine executed the tracking interpolation at 240 Hz. Three spatial allocation schemes were benchmarked:
\begin{itemize}
    \item \textbf{Uniform Allocation (Average)}: A naive baseline that evenly distributes $B_{total}/7$ bits to each joint.
    \item \textbf{Static LQR-Weighted Allocation}: A mathematically derived baseline utilizing the static Jacobian sensitivity matrix (computed at the home posture) to perform logarithmic water-filling bit assignment.
    \item \textbf{DRL Dynamic Allocation (Proposed)}: The context-aware PPO agent that continuously redistributes bits based on the real-time 3D spatial observation ($s_t$).
\end{itemize}
\noindent\textbf{1. Task-Oriented Waypoint Accuracy and Error Suppression}

Aligned with the philosophy of \textit{Pragmatic Communication}, the evaluation metric shifts focus from the rigid tracking of continous intermediate 
trajectories to ultimate \textbf{Task Success}. The tracking performance was quantified by measuring the steady-state 3D Euclidean distance 
deviation from the Ground Truth (GT) at two critical waypoints: the \textbf{Grasp Point} and the \textbf{Final Placement Point}. The averaged steady-state 
waypoint errors across the diverse bandwidth constraints are summarized in Table.~\ref{tab:qc}

\begin{table}[H]
  \centering
  \renewcommand{\arraystretch}{1.8} % 增加行高以匹配图片中较为宽松的间距
  \begin{tabular}{llcc}
    \toprule
    \textbf{Total Bandwidth} & \textbf{Allocation Scheme} & \textbf{Grasp Error (mm)} & \textbf{Placement Error (mm)} \\
    \midrule
    \textbf{14-bit} (Extreme) & Uniform (Avg) & 27.67 & 32.48 \\
                              & Static LQR    & 48.85 & 53.43 \\
                              & \textbf{DRL (Proposed)} & \textbf{15.65} & \textbf{23.82} \\
    \addlinespace
    \textbf{21-bit} (Severe)  & Uniform (Avg) & 19.50 & 19.32 \\
                              & Static LQR    & 12.98 & 12.47 \\
                              & \textbf{DRL (Proposed)} & \textbf{8.51}  & \textbf{11.20} \\
    \addlinespace
    \textbf{28-bit} (Moderate)& Uniform (Avg) & 8.24  & 7.49  \\
                              & Static LQR    & 5.83  & 7.28  \\
                              & \textbf{DRL (Proposed)} & \textbf{5.18}  & \textbf{3.08}  \\
    \addlinespace
    \textbf{56-bit} (Saturated)& Uniform (Avg) & 0.53  & 0.83  \\
                              & Static LQR    & 0.58  & 2.21  \\
                              & \textbf{DRL (Proposed)} & \textbf{0.45}  & \textbf{0.71}  \\
    \bottomrule
  \end{tabular}
  \caption{Quantitative comparison of the steady-state Euclidean deviation at critical waypoints across varying total bandwidth constraints ($B_{total}$).}
  \label{tab:qc}
\end{table}
As depicted in Tabel.~\ref{tab:qc}, the scalability test reveals a fascinating, highly non-linear relations between network constraints and spatial control performance, perfectly highlighting the limitations of traditional mathematical approaches.

\noindent\textbf{The Saturated Regime (56-bit)}: When network resources are abundant, providing an average of 8 bits per joint, quantization noise becomes 
negligible. All three allocation schemes reliably guide the end-effector to the target waypoints with sub-millimeter precision. In this regime, 
the sophisticated dynamic allocation offers only marginal gains over a naive uniform distribution.

\noindent\textbf{The Extreme Bottleneck and the Collapse of Static Models (14-bit)}: The most critical academic observation occurs at the 14-bit limit (averaging merely 2 bits per joint). 
Here, the mathematically sophisticated \textbf{Static LQR allocation exhibits a catastrophic failure}, yielding a massive Grasp Error of 48.85 mm--significantly 
worse than even the naive Uniform baseline (27.67 mm). This physical deviation far exceeds the tolerance of a standard robotic parallel gripper, guaranteeing a severe collision or a complete miss.

This phenomenon uncovers a fatal flaw in traditional time-invariant mathematical models: the Jacobian sensitivity matrix is highly state-dependent. The Static LQR weights were computed at the initial safe "home posture". 
However, when the manipulator stretches out to a distant grasp coordinate, its physical kinematics and non-linear leverage change completely. The Static LQR rigidly forces bits to joints that are no longer the most sensitive in this new posture, thereby actively sabotaging the system.
\noindent\textbf{2. Spatial Awareness and Graceful Degradation}
In stark contrast to the static baseline's collapse, the proposed \textbf{DRL dynamic allocator demonstrates absolute dominance} under the 14-bit constraint, compressing the Grasp Error to an impressive \textbf{15.65 mm} and the Placement Error to \textbf{23.82 mm}.

This superiority stems from the injection of 3D spatial awareness into the DRL's observation space. Unlike the rigid formula, the PPO agent 
ingests a 34-dimensional state vector that includes the real-time 3D coordinates of the end-effector and the target. Consequently, the neural 
network implicitly learns to compute the real-time Jacobian matrix.

When operating at the edge of the manipulator's workspace, the agent acts pragmatically: it deliberately starves the distal wrist joints to the absolute minimum limit of 1 bit, absorbing the resulting local quantization noise to accumulate enough bandwidth "currency" to secure 4 to 5 bits for the heavily loaded proximal joints. 
This phenomenon embodies the concept of graceful degradation. By proactively sacrificing low-sensitivity components based on real-time spatial context to guarantee high-precision transmission for critical links, the DRL allocator successfully navigates extreme network starvation.
\subsection{Simulation Scenario II: Underactuated Survival (Quadrotor UAV)}
To rigorously prove that the proposed DRL allocator acts as a universal, paradigm-agnostic JCC framework, a supplementary cyber-physical 
evaluation was conducted using a Quadrotor Unmanned Aerial Vehicle (UAV). Unlike the fully-actuated mechanical manipulator discussed in Section~\ref{sec:s1}--which simply halts or experiences steady-state 
deviations when bandwidth is insufficient--an underactuated quadrotor represents a strict "life-or-death" control scenario.
In such systems, severe quantization noise does not merely reduce precision; it leads to immediate aerodynamic stalling, loss of 
attitude control, and catastrophic crashing.
\subsubsection{Experimental Setup and the 24-Bit Extreme Bottleneck}
The UAV simulation was constructed using PyBullet physics engine. The state space of the quadrotor is represented by a 12-dimensional 
vector encompassing 3D positions, Euler angles (roll, pitch, yaw), and their respective linear and angular velocities.

To push the allocation algorithms to their absolute operational limits, the total channel capacity was severely choked to a strict $B_{total}=24$ 
bits per transmission (averaging a mere 2 bits). The communication frequency remained decoupled at 10 Hz over the JCC network, while the inner-loop 
PID flight controller operated at 240 Hz based on the Zero-Order Hold (ZOH) reconstructed quantized states.

To comprehensively test the robustness and context-adaptability of the allocation schemes, the evaluation was divided into two distinct,
highly hostile operational scenarios:
\begin{itemize}
    \item \textbf{Scenario A - Severe Wind Hovering (Wind Level: 2.0)}: The UAV is commanded to maintain a stationary hover while being subjected to a continuous, severe wind disturbance. This scenario tests the baseline physical survival capability under static spatial demands.
    \item \textbf{Scenario B - Dynamic Maneuvering under Strong Wind (Wind Level: 1.5)}: The UAV is commanded to maintain a hover for the first 5.0 seconds. At exactly $T = 5.0$ s, a sudden 2.5-meter waypoint displacement command is injected into the network. This evaluates the allocator's ability to dynamically switch semantic context between "attitude stabilization" and "positional navigation" under extreme duress.
\end{itemize}
\subsubsection{Extended Survivability and Graceful Degradation}
The survivability outcomes under these extreme cyber-physical constraints are quantitatively summarized in Table.~\ref{tab:uav}.

\begin{table}[H]
  \centering
  \footnotesize
  \renewcommand{\arraystretch}{1.8}
  \caption{Quantitative survival outcomes of the Quadrotor UAV under the 24-bit constraint in hostile environments. The DRL agent demonstrates equivalent performance in static hovering and significantly extends the survival window during dynamic maneuvering.}
  \begin{tabular}{lll}
    \toprule
    \textbf{Allocation Scheme} & \textbf{Scenario A: Severe Wind Hovering} & \textbf{Scenario B: Dynamic Maneuvering} \\
    \midrule
    Uniform (Average)       & \xmark Crashed at T = 5.2 s           & \xmark Crashed at T = 3.3 s          \\
    Static LQR              & \cmark Survived (\textgreater 20.0 s) & \xmark Crashed at T = 6.1  s (Survived 1.1s post-command)         \\
    \textbf{DRL (Proposed)} & \cmark Survived (\textgreater 20.0 s) & \begin{tabular}[c]{@{}l@{}}\xmark Crashed at T = 8.6 s (\textbf{Survived 3.6s post-command}) \end{tabular} \\
    \bottomrule
  \end{tabular}
  \label{tab:uav}
\end{table}
\noindent\textbf{1. Scenario A: The Emergence of Pragmatic Prioritization}

Under the severe wind conditions of Scenario A, the naive Uniform allocation scheme collapsed rapidly, crashing at $T=5.2$ seconds.
Distributing an equal 2 bits across all 12 dimensions failed to provide the necessary tracking resolution for critical roll ($\phi$) and pitch ($\theta$) 
axes, causing the internal PID controller to output erratic motor torques.

In contrast, both the Static LQR and the proposed DRL agent successfully maintained stable flight for the full 20-seconds evaluation 
duration. The Static LQR survived because its predefined, mathematically derived sensitivity formula heavily favored the attitude axes, which perfectly matched the requirements for a stationary, wind-resisting hover.
However, the DRL agent achieved this exact expert-level prioritization organically. Without relying on predefined sensitivity matrices, the PPO agent independently discovered the fundamental aerodynamic truth: absolute spatial coordinates ($X, Y$) must be deliberately starved of bandwidth to channel life-saving bits toward high-frequency attitude stabilization.

\noindent\textbf{2. Scenario B: Extended Survivability via Dynamic Borrowing}

The true superiority and context-awareness of the DRL framework emerged spectacularly during Scenario B. At $T = 5.0$ seconds, 
the sudden 2.5-meter maneuver command fundamentally altered the task dynamics, exposing the fatal flaw of rigid mathematical allocations.

In sharp contrast, the \textbf{DRL agent demonstrated remarkable Extended Survivability}. While the ultimate physical limitations 
of the 24-bit channel inevitably restrict infinite survival during such conflicting demands (executing a high-speed maneuver while flighting strong wind),
the DRL agent successfully extended the operational survival window to $T=8.6$ seconds, enduring $3\times$ longer than the Static LQR post-command.

This extended resilience is directly attributed to the DRL's \textbf{Context-Aware Dynamic Borrowing} capability. By continuously monitoring the observation 
space (which inherently includes real-time tracking errors), the DRL agent dynamically recognized the massive target displacement at $T = 5.0$ s. 
Instead of stubbornly holding the hovering strategy, it proactively extracted a few bits from the attitude axes to momentarily provide critical positional updates to the $X$-axis, while desperately attempting to balance aerodynamic stability.

This intelligent, real-time arbitration proves that the proposed DRL framework transcends rigid mathematical matrices. It is capable of 
executing \textit{Graceful Degradation}--squeezing out the absolute maximum utility from the Shannon channel capacity to extend the physical system's life,
a capability vital for emergency fail-safes in real-world autonomous systems.
\subsection{Scenario III: Digital Twin and Composite Stress Testing}
While the previous sections validated the fundamental algorithmic superiority of the proposed JCC framework on industrial-grade 
and highly dynamic models, real-world edge cyber-physical systems operate under significantly harsher realities. Low-cost customized 
manipulators, widely deployed as edge IoT actuators, frequently suffer from unmodeled environmental disturbances, payload fluctuations,
and severe network bottlenecks.

To rigorously validate the proposed DRL framework's deployability and its adaptability against "edge-case" hardware scenarios without 
risking catastrophic physical damage, a composite software-in-the-loop stress test was conducted utilizing a high-fidelity \textbf{Digital Twin}.
\subsubsection{Digital Twin Modeling of the Edge Manipulator}
Instead of relying on idealized industrial models, a precise Digital Twin of a low-cost, custom 5-Degree-of-Freedom (5-DOF) serial 
manipulator (based on the Jibot1 architecture) was constructed using the Unified Robot Description Format (URDF).
\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/cmp.png}
    \caption{Architectural comparison between the physical custom edge manipulator (right) and its high-fidelity Digital Twin rendered in the PyBullet physics engine (left).}
\end{figure}
To guarantee simulation fidelity, the kinematic and dynamic parameters of the URDF model were empirically measured and calibrated against 
the physical hardware. The specific configuration parameters are detailed as follows:
\begin{enumerate}
    \item \textbf{Kinematic Link Lengths}: The spatial topology is defined by the measured segment lengths: Base elevation $L_0 = 100 \text{ mm}$  Main arm $L_1 = 105 \text{ mm}$, Forearm $L_2 = 75 \text{ mm}$, Wrist $L_3 = 65 \text{ mm}$, and the terminal Gripper assembly $L_g = 120 \text{ mm}$.
    \item \textbf{Dynamic Mass Distribution}: The total system mass is meticulously calibrated $m_{total} \approx 1.24\text{ kg}$. The inertial tensors were approximated using bounding geometries, ensuring the heavy base and shoulder joints accurately reflect the physical payload burdens.
    \item \textbf{}
\end{enumerate}